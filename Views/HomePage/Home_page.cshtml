@model LapTrinhWebBanHang.Models.Product
@{
    ViewBag.Title = "HANGMUSPORTS";
    Layout = "~/Views/Shared/_MasterLayout.cshtml";
}
<link rel="stylesheet" href="~/Content/Home_page/introduce_homePage.css" />
<link rel="stylesheet" href="~/Content/Home_page/container_homePage.css">
<!-- INTRO -->
<div class="introduce">
    <div class="slide">
        <div id="carouselExample" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img src="~/Image/thumbnail/PhongThayDoMU.jpg"
                         class="d-block w-100"
                         alt="Slide 1" />
                </div>
                <div class="carousel-item">
                    <img src="~/Image/thumbnail/thumb3.jpg"
                         class="d-block w-100"
                         alt="Slide 2" />
                </div>
                <div class="carousel-item">
                    <img src="~/Image/thumbnail/thumb4jpg.jpg"
                         class="d-block w-100"
                         alt="Slide 3" />
                </div>
            </div>
            <button class="carousel-control-prev"
                    type="button"
                    data-bs-target="#carouselExample"
                    data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next"
                    type="button"
                    data-bs-target="#carouselExample"
                    data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
            <div class="carousel-indicators">
                <button type="button" data-bs-target="#carouselExample" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
                <button type="button" data-bs-target="#carouselExample" data-bs-slide-to="1" aria-label="Slide 2"></button>
                <button type="button" data-bs-target="#carouselExample" data-bs-slide-to="2" aria-label="Slide 3"></button>
            </div>
        </div>
    </div>
    <div class="commit">
        <div class="row">
            <div class="col-sm-1 col-md-4">
                <div class="credit">
                    <i class="fa-duotone fa-solid fa-credit-card commit-icon"></i>
                    <div class="description commit-size">
                        <h3>CAM KẾT CHÍNH HÃNG</h3>
                        <h5>100% Authentic</h5>
                        <p>Cam kết sản phẩm chính hãng</p>
                    </div>
                </div>
            </div>
            <div class="col-sm-1 col-md-4">
                <div class="delivery commit-size">
                    <i class="fa-duotone fa-solid fa-truck commit-icon"></i>
                    <div class="description">
                        <h3>GIAO HÀNG HỎA TỐC</h3>
                        <h5>Express delivery</h5>
                        <p>SHIP hỏa tốc 1h nhận hàng trong nội thành HCM</p>
                    </div>
                </div>
            </div>
            <div class="col-sm-1 col-md-4">
                <div class="hotline">
                    <i class="fa-duotone fa-solid fa-phone-volume commit-icon"></i>
                    <div class="description">
                        <h3>HỖ TRỢ 24/24</h3>
                        <h5>Supporting 24/24</h5>
                        <p>Gọi ngay</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- CONTENT -->
<div class="container">
    <!-- NEW PRODUCTS -->
    <div class="new-products">
        <h2>Sản phẩm mới</h2>
        <div class="row thumb">
            <img src="~/Image/thumbnail/bannerMain.png" />
        </div>
        <div class="new-products-list row" id="new-products-list">
        </div>
        <div class="number-list">
            <ul>
                <li>1</li>
                <li>2</li>
                <li>3</li>
                <li>4</li>
                <li>></li>
                <li>>></li>
            </ul>
        </div>
    </div>

    <!-- HOT PRODUCTS -->
    <div class="hot-products">
        <h2>NIKE</h2>
        <div class="row thumb">
            <img class="col-md-6" src="~/Image/thumbnail/NikePack/NikeRiSingGemPack.jpg" />
            <img class="col-md-6" src="~/Image/thumbnail/NikePack/NikePinkPack.jpg" />
        </div>
        <div class="hot-products-list row" id="nike-products-list">
        </div>
    </div>

    <div class="hot-products">
        <h2>ADIAS</h2>
        <div class="row thumb">
            <img class="col-md-4" src="~/Image/thumbnail/AdidasPack/AdidasHeatspawnPack.jpg" />
            <img class="col-md-4" src="~/Image/thumbnail/AdidasPack/AdidasUniforiaPack.jpg" />
            <img class="col-md-4" src="~/Image/thumbnail/AdidasPack/AdidasWhitePack.jpg" />
        </div>
        <div class="hot-products-list row" id="adidas-products-list">
        </div>
    </div>
    <div class="hot-products">
        <h2>PUMA</h2>
        <div class="row thumb">
            <img class="col-md-4" src="~/Image/thumbnail/PumaPack/PumaBrilliancePack.jpg" />
            <img class="col-md-4" src="~/Image/thumbnail/PumaPack/PumaLightsOutPack.jpg" />
            <img class="col-md-4" src="~/Image/thumbnail/PumaPack/PumaRushPack.jpg" />
        </div>
        <div class="hot-products-list row" id="puma-products-list">
        </div>
    </div>

    <!--  jersey -->
    <div class="jersey" id="jersey">
        <h2>Áo đấu</h2>
        <div class="row thumb">
            <img class="col-md-6" src="~/Image/thumbnail/JerseyThumb/MUjersey.jpg" />
            <img class="col-md-6" src="~/Image/thumbnail/JerseyThumb/TOTjersey.jpg" />
        </div>
        <div class="jersey-list row" id="jersey-list">
        </div>
    </div>

    <!-- Brands -->
    <div class="brands">
        <h2>Thương hiệu </h2>
        <div class="brands-list row">
            <!--  Brands-item -->
            <div class="brand-item col-lg-4 col-md-6 col-sm-12">
                <div class="product-image">
                    <img src="~/Image/thumbnail/brand_banner_1_11zon.png" alt="" />
                </div>
                <div class="product-content">
                    <h6>GIÀY BÓNG ĐÁ NIKE</h6>
                </div>
            </div>
            <!--  Brands-item -->
            <div class="brand-item col-lg-4 col-md-6 col-sm-12">
                <div class="product-image">
                    <img src="~/Image/thumbnail/brand_banner_2.jpg" alt="" />
                </div>
                <div class="product-content">
                    <h6>GIÀY BÓNG ĐÁ ADIDAS</h6>
                </div>
            </div>
            <!--  Brands-item -->
            <div class="brand-item col-lg-4 col-md-6 col-sm-12">
                <div class="product-image">
                    <img src="~/Image/thumbnail/brand_banner_3.jpg" alt="" />
                </div>
                <div class="product-content">
                    <h6>GIÀY BÓNG ĐÁ PUMA</h6>
                </div>
            </div>
        </div>
    </div>

    <!--  Instruct -->
    <div style="margin-bottom: 20px;" class="instruct" id="instruct">
        <h2>Hướng dẫn chọn size</h2>
        <div class="instruct-list row">
            <!--  Instruct-item -->
            <div class="brand-item col-md-4 col-12">
                <div class="product-image">
                    <img src="~/Image/thumbnail/instruct 2.jpg" alt="" />
                </div>
                <div class="product-content">
                    <h6 style="opacity: 0.6;">CHỌN SIZE GIÀY NIKE,ADIDAS</h6>
                </div>
            </div>
            <!--  Instruct-item -->
            <div class="brand-item col-md-4 col-12">
                <div class="product-image">
                    <img src="~/Image/thumbnail/instruct 3.jpg" alt="" />
                </div>
                <div class="product-content">
                    <h6 style="opacity: 0.6;">CÁCH ĐO CỠ CHÂN VÀ XÁC ĐỊNH SIZE VIỆT NAM, US, UK CHUẨN</h6>
                </div>
            </div>
            <!--  Instruct-item -->
            <div class="brand-item col-md-4 col-12">
                <div class="product-image">
                    <img src="~/Image/thumbnail/instruct 4.jpg" alt="" />
                </div>
                <div class="product-content">
                    <h6 style="opacity: 0.6;">HƯỚNG DẪN CÁCH VỆ SINH GIÀY</h6>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    function loadNewProducts() {
    fetch('@Url.Action("GetAllProductsJson", "HomePage")')
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                const products = data.data;
                const newproductList = document.getElementById('new-products-list');
                newproductList.innerHTML = ''; // Làm trống danh sách trước khi thêm mới

                // Giới hạn hiển thị chỉ 8 sản phẩm đầu tiên
                const limitedProducts = products.slice(0, 8);

                // Duyệt qua danh sách sản phẩm giới hạn và thêm vào HTML
                limitedProducts.forEach(product => {
                    const productItem = document.createElement('div');
                    productItem.classList.add('product-item','col-xl-3' ,'col-lg-4', 'col-sm-6','col-12');

                    // Bao cả phần product-item trong thẻ a để khi click vào toàn bộ sản phẩm sẽ chuyển tới ProductPage
                    productItem.innerHTML = `
                        <a style="color: black; text-decoration: none;" href="@Url.Action("ProductPage", "ProductPage")/${product.ProductID}">
                            <div class="product-image">
                                <span class="new-tag tag">New</span>
                                <img src="@Url.Content("~/Image/product-image/${product.ImageURL}")" alt="${product.ProductName}" />
                            </div>
                            <div class="product-content">
                                <h6>${product.ProductName}</h6>
                                <p class="description-product">${product.Description}</p>
                                <p class="price">${product.Price.toLocaleString()}</p>
                            </div>
                        </a>
                    `;
                    newproductList.appendChild(productItem);
                });
            } else {
                console.log('Không thể tải sản phẩm');
            }
        })
        .catch(error => console.log('Lỗi:', error));
}


   function loadHotProducts() {
    fetch('@Url.Action("GetAllProductsJson", "HomePage")')
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                const products = data.data;
                const hotproductList = document.getElementById('new-products-list');
                hotproductList.innerHTML = ''; // Làm trống danh sách trước khi thêm mới

                // Giới hạn hiển thị chỉ 4 sản phẩm đầu tiên
                const limitedProducts = products.slice(0, 4);

                // Duyệt qua danh sách sản phẩm giới hạn và thêm vào HTML
                limitedProducts.forEach(product => {
                    const productItem = document.createElement('div');
                    productItem.classList.add('product-item', 'col-xl-3', 'col-lg-4', 'col-sm-6', 'col-12');

                    // Bao cả phần product-item trong thẻ a để khi click vào toàn bộ sản phẩm sẽ chuyển tới ProductPage
                    productItem.innerHTML = `
                        <a style="color: black; text-decoration: none;" href="@Url.Action("ProductPage", "ProductPage")/${product.ProductID}">
                            <div class="product-image">
                                <span class="new-tag tag">New</span>
                                <img src="@Url.Content("~/Image/product-image/${product.ImageURL}")" alt="${product.ProductName}" />
                            </div>
                            <div class="product-content">
                                <h6>${product.ProductName}</h6>
                                <p class="description-product">${product.Description}</p>
                                <p class="price">${product.Price.toLocaleString()}</p>
                            </div>
                        </a>
                    `;
                    hotproductList.appendChild(productItem);
                });
            } else {
                console.log('Không thể tải sản phẩm');
            }
        })
        .catch(error => console.log('Lỗi:', error));
    }

       function loadNikeProducts() {
    fetch('@Url.Action("GetAllNike", "HomePage")')
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                const products = data.data;
                const hotproductList = document.getElementById('nike-products-list');
                hotproductList.innerHTML = ''; // Làm trống danh sách trước khi thêm mới

                // Giới hạn hiển thị chỉ 4 sản phẩm đầu tiên
                const limitedProducts = products.slice(0, 4);

                // Duyệt qua danh sách sản phẩm giới hạn và thêm vào HTML
                limitedProducts.forEach(product => {
                    const productItem = document.createElement('div');
                    productItem.classList.add('product-item', 'col-xl-3', 'col-lg-4', 'col-sm-6', 'col-12');

                    // Bao cả phần product-item trong thẻ a để khi click vào toàn bộ sản phẩm sẽ chuyển tới ProductPage
                    productItem.innerHTML = `
                        <a style="color: black; text-decoration: none;" href="@Url.Action("ProductPage", "ProductPage")/${product.ProductID}">
                            <div class="product-image">
                                <span class="new-tag tag">New</span>
                                <img src="@Url.Content("~/Image/product-image/${product.ImageURL}")" alt="${product.ProductName}" />
                            </div>
                            <div class="product-content">
                                <h6>${product.ProductName}</h6>
                                <p class="description-product">${product.Description}</p>
                                <p class="price">${product.Price.toLocaleString()}</p>
                            </div>
                        </a>
                    `;
                    hotproductList.appendChild(productItem);
                });
            } else {
                console.log('Không thể tải sản phẩm');
            }
        })
        .catch(error => console.log('Lỗi:', error));
    }

           function loadAdidasProducts() {
    fetch('@Url.Action("GetAllAdidas", "HomePage")')
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                const products = data.data;
                const hotproductList = document.getElementById('adidas-products-list');
                hotproductList.innerHTML = ''; // Làm trống danh sách trước khi thêm mới

                // Giới hạn hiển thị chỉ 4 sản phẩm đầu tiên
                const limitedProducts = products.slice(0, 4);

                // Duyệt qua danh sách sản phẩm giới hạn và thêm vào HTML
                limitedProducts.forEach(product => {
                    const productItem = document.createElement('div');
                    productItem.classList.add('product-item', 'col-xl-3', 'col-lg-4', 'col-sm-6', 'col-12');

                    // Bao cả phần product-item trong thẻ a để khi click vào toàn bộ sản phẩm sẽ chuyển tới ProductPage
                    productItem.innerHTML = `
                        <a style="color: black; text-decoration: none;" href="@Url.Action("ProductPage", "ProductPage")/${product.ProductID}">
                            <div class="product-image">
                                <span class="new-tag tag">New</span>
                                <img src="@Url.Content("~/Image/product-image/${product.ImageURL}")" alt="${product.ProductName}" />
                            </div>
                            <div class="product-content">
                                <h6>${product.ProductName}</h6>
                                <p class="description-product">${product.Description}</p>
                                <p class="price">${product.Price.toLocaleString()}</p>
                            </div>
                        </a>
                    `;
                    hotproductList.appendChild(productItem);
                });
            } else {
                console.log('Không thể tải sản phẩm');
            }
        })
        .catch(error => console.log('Lỗi:', error));
    }

               function loadPumaProducts() {
    fetch('@Url.Action("GetAllPuma", "HomePage")')
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                const products = data.data;
                const hotproductList = document.getElementById('puma-products-list');
                hotproductList.innerHTML = ''; // Làm trống danh sách trước khi thêm mới

                // Giới hạn hiển thị chỉ 4 sản phẩm đầu tiên
                const limitedProducts = products.slice(0, 4);

                // Duyệt qua danh sách sản phẩm giới hạn và thêm vào HTML
                limitedProducts.forEach(product => {
                    const productItem = document.createElement('div');
                    productItem.classList.add('product-item', 'col-xl-3', 'col-lg-4', 'col-sm-6', 'col-12');

                    // Bao cả phần product-item trong thẻ a để khi click vào toàn bộ sản phẩm sẽ chuyển tới ProductPage
                    productItem.innerHTML = `
                        <a style="color: black; text-decoration: none;" href="@Url.Action("ProductPage", "ProductPage")/${product.ProductID}">
                            <div class="product-image">
                                <span class="new-tag tag">New</span>
                                <img src="@Url.Content("~/Image/product-image/${product.ImageURL}")" alt="${product.ProductName}" />
                            </div>
                            <div class="product-content">
                                <h6>${product.ProductName}</h6>
                                <p class="description-product">${product.Description}</p>
                                <p class="price">${product.Price.toLocaleString()}</p>
                            </div>
                        </a>
                    `;
                    hotproductList.appendChild(productItem);
                });
            } else {
                console.log('Không thể tải sản phẩm');
            }
        })
        .catch(error => console.log('Lỗi:', error));
}


    // Hàm để tải danh sách áo đấu từ Controller
function loadJerseys() {
    fetch('@Url.Action("GetAllFootballJersey", "HomePage")')
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                const products = data.data;
                const jerseyList = document.getElementById('jersey-list');
                jerseyList.innerHTML = ''; // Làm trống danh sách trước khi thêm mới

                // Duyệt qua danh sách sản phẩm và thêm vào HTML
                products.forEach(product => {
                    const productItem = document.createElement('div');
                    productItem.classList.add('product-item','col-xl-3' ,'col-lg-4', 'col-sm-6','col-12');

                    // Bao cả phần product-item trong thẻ a để khi click vào toàn bộ sản phẩm sẽ chuyển tới ProductPage
                    productItem.innerHTML = `
                    <a style="color: black; text-decoration: none;" href="@Url.Action("ProductPage", "ProductPage")/${product.ProductID}">
                        <div class="product-image">
                            <img src="@Url.Content("~/Image/product-image/${product.ImageURL}")" alt="${product.ProductName}" />
                        </div>
                        <div class="product-content">
                            <h6>${product.ProductName}</h6>
                            <p class="description-product">${product.Description}</p>
                            <p class="price">${product.Price.toLocaleString()}</p>
                        </div>
                    </a>
                    `;
                    jerseyList.appendChild(productItem);
                });
            } else {
                console.log('Không thể tải áo đấu');
            }
        })
        .catch(error => console.log('Lỗi:', error));
}

    // Gọi hàm loadProducts khi trang được tải
    document.addEventListener('DOMContentLoaded', function () {
        loadNewProducts();
        loadNikeProducts()
        loadAdidasProducts();
        loadPumaProducts();
        loadJerseys();
    });

    //window.onscroll = function () {
    //    makeDivNav();
    //}

    //// Di chuyển thanh NAV khi cuộn trang
    //function makeDivNav() {
    //    var fixedNav = document.querySelector(".nav"); // Sử dụng querySelector thay vì jQuery
    //    var navPosition = fixedNav.offsetTop;
    //    const scrollPosition = document.body.scrollTop || document.documentElement.scrollTop;

    //    if (scrollPosition > navPosition) {
    //        fixedNav.classList.add("fixed");
    //    } else {
    //        fixedNav.classList.remove("fixed");
    //    }
    //}
</script>






